# Build intermediates (the shipped DLL lives in MasterBillList/Assemblies and IS tracked)
**/obj/
**/bin/
*.user

# OS / editor cruft
.DS_Store
.vs/
.idea/
*.swp

# RimWorld: keep About/PublishedFileId.txt tracked once it exists — it's the
# Steam Workshop item ID and must persist to update the same item later.
